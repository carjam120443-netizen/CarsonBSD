# Base System

CarsonBSD base-system integration and configuration lives here.

The OpenBSD base remains the foundation of CarsonBSD; this directory is for CarsonBSD-specific integration rather than unnecessary duplication of upstream source.
