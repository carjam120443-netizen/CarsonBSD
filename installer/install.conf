# CarsonBSD unattended installer profile
# Intended for a dedicated target disk. The OpenBSD installer consumes
# this configuration through autoinstall(8).

System hostname = carsonbsd
Password for root = *
Allow root ssh login = no
Start sshd(8) by default = no
Which disk is the root disk = sd0
Use (W)hole disk MBR, whole disk GPT or (E)dit? = GPT
Use (A)uto layout, (E)dit auto layout, or create (C)ustom layout? = A
Which disk is the root disk = sd0
Location of sets = http
HTTP Server = cdn.openbsd.org
HTTP Server directory = /pub/OpenBSD/7.9/amd64
Set name(s) = -x*
Continue without verification = no
Change the default console to = com0
What timezone are you in? = US/Eastern
