# CarsonBSD Tools

Home for CarsonBSD-specific command-line utilities.

Planned tools include:

- `cbfetch` — CarsonBSD system information and branding
- `cbpkg` — future package/repository helper
- `cbconfig` — system configuration helper
